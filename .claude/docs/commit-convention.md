# 커밋 컨벤션

커밋 메시지는 `type(scope): message` 형식을 따른다.

- **type**: 어떤 행위인지를 나타낸다. `feat`, `fix`, `refactor`, `chore`
- **scope**: 어떤 영역을 건드렸는지를 나타낸다. `backend`, `frontend`, `docs`. 영역이 겹치면 `-`로 조합한다 (예: `frontend-docs`)

예시:

```
feat(backend): 유저 기능 추가
fix(frontend): 로그인 버튼 클릭 오류 수정
feat(docs): 프로젝트 개요 문서 추가
feat(frontend-docs): 화면 스크린샷 추가
```

## 커밋 범위

한 커밋에는 Claude가 해당 세션에서 직접 작업한 변경사항만 포함한다. 사용자가 직접 수정했거나 다른 도구가 만든 변경사항이 워킹 트리에 같이 있어도 임의로 묶어서 커밋하지 않는다. 관련 없는 변경사항이 있으면 커밋하지 않고 사용자에게 알린다.

## 커밋 타이밍

작업이 끝났다고 바로 커밋하지 않는다. 항상 사용자의 검토/응답을 먼저 기다리고, 사용자가 명시적으로 커밋을 요청했을 때만 커밋한다. "빌드/테스트 통과했으니 커밋한다" 같은 자체 판단으로 먼저 커밋하지 않는다.
